# These will be automatically created by Neon Launchpad (https://neon.new).
# You will also need to
DATABASE_URL=
DATABASE_URL_POOLER=

# Clerk — required for admin mutations in production. The backend verifies
# the session on each /api/trpc request and extracts the user's primary
# email, which is matched against ADMIN_EMAILS below.
CLERK_SECRET_KEY=
VITE_CLERK_PUBLISHABLE_KEY=

# Comma-separated whitelist of emails allowed to call admin mutations.
# Empty in non-production: admin mutations are reachable without sign-in
# (dev convenience). Empty in production: all mutations rejected.
# See docs/admin-auth.md.
ADMIN_EMAILS=
